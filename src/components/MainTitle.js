const MainTitle = ({ title }) => {
  return (
    <div className="text-center mb-20">
      <h2 className="text-2xl sm:text-4xl text-white font-bold capitalize ">
        {title}
      </h2>
    </div>
  );
};

export default MainTitle;
